azuread_groups_membership = {
  caf_launchpad_maintainer = { # AD group key

    object_ids = {
      logged_in = {
        keys = ["user"]
      }
    }
  }
  grp-azure-devops = { # AD group key
    object_ids = {
      logged_in = {
        keys = ["user"]
      }
    }
  }
}