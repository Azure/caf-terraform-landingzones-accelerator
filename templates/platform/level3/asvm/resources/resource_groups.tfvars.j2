resource_groups = {
{% for key, resource_group in asvm_resources.resource_groups.items() %}
  {{ key }} = {
    name   = "{{ resource_group.name }}"
    region = "{{ resource_group.region_key }}"
  }
{% endfor %}
}