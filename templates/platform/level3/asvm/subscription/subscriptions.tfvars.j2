subscriptions = {

{% for key in asvm_subscription.subscriptions.keys() %}
  {{ key }} = {
    name                    = "{{ asvm_subscription.subscriptions[key].name }}"
    billing_account_name    = "{{ config.billing_subscription_role_delegations.billing_account_name }}"
    enrollment_account_name = "{{ config.billing_subscription_role_delegations.enrollment_account_name }}"
    management_group_id     = "{{ config.platform_core_setup.enterprise_scale.management_group_prefix }}-{{ asvm_subscription.subscriptions[key].management_group_suffix }}"
    workload                = "{{ asvm_subscription.subscriptions[key].workload | default('Production') }}"
    tags                    = {
{% for tag_key in asvm_subscription.subscriptions[key].tags %}
      {{ tag_key }} = "{{ asvm_subscription.subscriptions[key].tags[tag_key] }}"
{% endfor %}
    }
  }
{% endfor %}

}