virtual_wans = {
{% for key, vwan in connectivity.virtual_wans.items() %}
  "{{key}}" = {
    resource_group_key = "{{ vwan.resource_group_key }}"
    name               = "{{ vwan.name }}"
    region             = "{{ vwan.region_key }}"
  }
{% endfor %}
}
