landingzone = {
  backend_type        = "{{ caf_terraform.launchpad.backend_type | default("azurerm")}}"
  global_settings_key = "{{ tfstates.launchpad.lz_key_name }}"
  level               = "{{ tfstates.identity.level }}"
  key                 = "{{ tfstates.identity.lz_key_name }}"
  tfstates = {
    {{ tfstates.launchpad.lz_key_name }} = {
      level   = "lower"
      tfstate = "{{ tfstates.launchpad.tfstate }}"
    }
  }
}
